#spring.jpa.hibernate.ddl-auto=update 
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect 
#spring.datasource.url=jdbc:${DB_URL}${DB_HOST}/${DATABASE}?createDatabaseIfNotExist=true&autoReconnect=true&useSSL=false&reconnect=true 
#spring.datasource.username=${DB_USER} 
#spring.datasource.password=${PASSWORD} 
#spring.data.rest.basePath=/api 
#spring.jpa.show-sql=true 
#server.port=${PORT}

# ==============================
# JPA / Hibernate
# ==============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true

# ==============================
# Tietokantayhteys
# ==============================
spring.datasource.url=jdbc:mysql://${DB_HOST}:${DB_PORT}/${DATABASE}?useSSL=true&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USER}
spring.datasource.password=${PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==============================
# Spring Data REST
# ==============================
spring.data.rest.basePath=/api

# ==============================
# Palvelimen portti
# ==============================
server.port=${PORT}


